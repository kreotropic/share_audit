# Excluded from the App Store tarball (krankerl package).
# Ships: appinfo/, lib/, js/, css/, img/, l10n/, templates/,
# README.md, CHANGELOG.md, ROADMAP.md, LICENSE.

# Dependencies and sources (compiled js/ ships instead)
node_modules
vendor
src
build

# Tests and CI
tests
phpunit.xml
.phpunit.result.cache
.github

# Dev/build config
webpack.config.js
babel.config.js
package.json
package-lock.json
composer.json
composer.lock
krankerl.toml

# Repo housekeeping
.git
.gitignore
.nextcloudignore
.vscode

# Local dev scripts and artifacts
_*.php
js/*.map
l10n/*.bak

# Screenshots ship via GitHub raw URLs in info.xml, not in the tarball
screenshots
